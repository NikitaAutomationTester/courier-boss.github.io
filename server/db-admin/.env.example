# Публикация порта админки БД (хост:порт контейнера nginx → 80)
# Только localhost (рекомендуется + SSH-туннель или VPN):
DB_ADMIN_PUBLISH=127.0.0.1:9080
#
# Открыть на всех интерфейсах (только если есть файрвол и желательно TLS на хосте):
# DB_ADMIN_PUBLISH=0.0.0.0:9080
